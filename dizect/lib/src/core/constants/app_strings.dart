class AppStrings {
  const AppStrings._();

  static const appName = 'Distributed Storage';
  static const welcomeTitle = 'Distributed storage, structured for scale.';
  static const welcomeSubtitle =
      'A clean starting point for syncing nodes, managing files, and building a resilient platform.';
}
